mcp-migration-check: scanned 3 Python files under examples/before

server.py
  line 19  [THIS WILL BREAK]  (Confirmed)  R3 - Hand-rolled transport missing Mcp-Method / Mcp-Name
      > async def app(scope, receive, send):
      The 2026-07-28 update requires the Mcp-Method and Mcp-Name headers on every
      Streamable HTTP POST request (SEP-2243). This only applies to hand-rolled
      MCP-over-HTTP transport code - servers using the official SDK's built-in
      Streamable HTTP transport get this handled for them. If you wrote your own
      transport wiring instead of using the SDK's, add both headers.
      Source: https://modelcontextprotocol.io/specification/2026-07-28/changelog.md (checked 2026-07-28)

  line 24  [THIS WILL BREAK]  (Confirmed)  R1 - Hand-rolled reading of the old session header
      > session_id = headers.get("mcp-session-id")
      The 2026-07-28 update removes protocol-level sessions and the Mcp-Session-Id
      header from the Streamable HTTP transport (SEP-2567). Reading the header
      directly by name means your code bypasses the SDK's own transport and
      session handling entirely, and is relying on the removed session model by
      hand. Replace it with an explicit, server-minted handle passed as an
      ordinary tool argument.
      Source: https://modelcontextprotocol.io/specification/2026-07-28/changelog.md (checked 2026-07-28)

  line 31  [THIS WILL BREAK]  (Confirmed)  R2 - Session-keyed server memory
      > BASKETS.setdefault(session_id, []).append(item)
      Servers that need cross-call state must use an explicit, server-minted
      handle passed back as an ordinary tool argument (SEP-2567), instead of
      memory keyed to a session the server remembers on its own.
      Source: https://modelcontextprotocol.io/specification/2026-07-28/changelog.md (checked 2026-07-28)

  line 32  [THIS WILL BREAK]  (Confirmed)  R2 - Session-keyed server memory
      > result = {"basket": BASKETS[session_id]}
      Servers that need cross-call state must use an explicit, server-minted
      handle passed back as an ordinary tool argument (SEP-2567), instead of
      memory keyed to a session the server remembers on its own.
      Source: https://modelcontextprotocol.io/specification/2026-07-28/changelog.md (checked 2026-07-28)

  line 35  [THIS WILL BREAK]  (Confirmed)  R5 - Hand-written use of a renumbered MCP error code
      > error_code = -32002
      The 2026-07-28 update's error-code allocation policy renumbers four specific
      JSON-RPC error codes: -32001 (HeaderMismatch) to -32020, -32002 (resource
      not found) to -32602, -32003 (MissingRequiredClientCapability) to -32021,
      and -32004 (UnsupportedProtocolVersion) to -32022. Code that hardcodes one
      of the four old numbers will silently stop matching once the other side
      upgrades - use the SDK's named error constants instead. Other codes in
      -32000..-32019 are explicitly grandfathered as implementation-defined and
      safe to keep.
      Source: https://modelcontextprotocol.io/specification/2026-07-28/changelog.md (checked 2026-07-28)

  line 38  [THIS WILL BREAK]  (Confirmed)  R2 - Session-keyed server memory
      > result = {"basket": BASKETS[session_id]}
      Servers that need cross-call state must use an explicit, server-minted
      handle passed back as an ordinary tool argument (SEP-2567), instead of
      memory keyed to a session the server remembers on its own.
      Source: https://modelcontextprotocol.io/specification/2026-07-28/changelog.md (checked 2026-07-28)

tools/legacy_handlers.py
  line 28  [THIS WILL BREAK]  (Confirmed)  R6 - SSE resumability opt-in (event_store)
      > resumable_mcp = FastMCP("basket-server-legacy", event_store=BasketEventStore())
      The 2026-07-28 update removes SSE stream resumability and message redelivery
      (the Last-Event-ID header and SSE event IDs) from the Streamable HTTP
      transport entirely (SEP-2575). A broken response stream now loses the in-
      flight request; clients must re-issue it as a new request with a new request
      ID. Passing an event_store to opt into resumability configures a mechanism
      the new spec no longer supports - remove it.
      Source: https://modelcontextprotocol.io/specification/2026-07-28/changelog.md (checked 2026-07-28)

  line 33  [THIS WILL BREAK]  (Confirmed)  R7 - Old-style resources/subscribe or resources/unsubscribe handler
      > @server.subscribe_resource()
      The 2026-07-28 update replaces the HTTP GET endpoint and resources/subscribe
      / resources/unsubscribe with a single subscriptions/listen long-lived stream
      (SEP-2575). A server still registering a handler for the old subscribe or
      unsubscribe request is implementing a request type the new spec no longer
      has - migrate to subscriptions/listen.
      Source: https://modelcontextprotocol.io/specification/2026-07-28/changelog.md (checked 2026-07-28)

  line 38  [THIS WILL BREAK]  (Confirmed)  R7 - Old-style resources/subscribe or resources/unsubscribe handler
      > @server.unsubscribe_resource()
      The 2026-07-28 update replaces the HTTP GET endpoint and resources/subscribe
      / resources/unsubscribe with a single subscriptions/listen long-lived stream
      (SEP-2575). A server still registering a handler for the old subscribe or
      unsubscribe request is implementing a request type the new spec no longer
      has - migrate to subscriptions/listen.
      Source: https://modelcontextprotocol.io/specification/2026-07-28/changelog.md (checked 2026-07-28)

  line 43  [THIS WILL BREAK]  (Confirmed)  R8 - Old-style logging/setLevel request handler
      > @server.set_logging_level()
      The 2026-07-28 update removes the logging/setLevel request entirely
      (SEP-2575); log level is now set per-request via a _meta key instead. A
      server still registering a handler for this request is implementing a
      request type the new spec no longer has. This is distinct from R4 (consuming
      the deprecated Logging capability, a 12-month grace period) - this is the
      removed request handler itself, with no grace period.
      Source: https://modelcontextprotocol.io/specification/2026-07-28/changelog.md (checked 2026-07-28)

tools/logging_setup.py
  line 18  [Worth checking]  (Confirmed)  R4 - Phased-out capability: Roots / Sampling / Logging
      > await ctx.session.send_log_message("debug", "Basket server debug logging enabled.")
      The official changelog now lists Roots, Sampling, and Logging as deprecated
      (SEP-2577), with a minimum twelve-month grace period before removal -
      suggested migrations are tool parameters/resource URIs/server config instead
      of Roots, direct LLM provider API calls instead of Sampling, and stderr or
      OpenTelemetry instead of Logging. This will not break on July 28, but plan
      to move off it.
      Source: https://modelcontextprotocol.io/specification/2026-07-28/changelog.md (checked 2026-07-28)

Summary: 10 will break, 1 worth checking, 0 needs manual check, 0 files skipped
